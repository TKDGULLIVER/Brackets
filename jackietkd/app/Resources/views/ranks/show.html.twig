{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ranks</h1>

    <table>
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ rank.name }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ rank.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ranks_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('ranks_edit', { 'id': rank.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
