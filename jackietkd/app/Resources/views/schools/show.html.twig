{% extends 'base.html.twig' %}

{% block body %}
    <h1>Schools</h1>

    <table>
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ school.name }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ school.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('schools_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('schools_edit', { 'id': school.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
